dist
# Scratch output from the SSR pass in scripts/prerender.mjs. The script cleans
# it up itself, so it only survives a build that died partway through.
.prerender
.wrangler
.output
.vercel
.netlify
.vinxi
app.config.timestamp_*.js

# Environment
.env
.env*.local
.dev.vars
.dev.vars.*

# dependencies
/node_modules

# IDEs and editors
/.idea
.project
.classpath
*.launch
.settings/

# Temp
gitignore

# System Files
.DS_Store
Thumbs.db
